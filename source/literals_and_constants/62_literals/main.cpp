/*

The C++20 Masterclass: From Fundamentals to Advanced

*/

#include <iostream>


int main(){
    
    // Notes:
    // literal: data that is directly represented in code wihtout going through some 
    // without going through some other variable stored in memory
    // const: a read only variable. can't assign data to it
    // constexper> a constant that has the pontential to be evaluated at the compile time 
    // constinit: a variable that should be initialized with a constant or literal at compile time
    
    
    int value {10};           // 10 value is a literal
    char char_literal {'c'};    // 'c' is a literal 


    std::cout << "signed_long_long_int" << std::endl;

    // 
    


    return 0;
}